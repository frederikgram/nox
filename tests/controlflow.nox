a: int = 0;

while(a < 10) {
    if(a % 2 == 0) {
        break;
    } else {
        continue;
    }

    // Dummy code, never gets executed
    a = a + 1;
}

// break; // Error: A_BREAK outside of loop
// continue; // Error: A_CONTINUE outside of loop

c: int = 0;